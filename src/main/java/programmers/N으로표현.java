package programmers;


public class N으로표현 {
    //N=5, number=12, return=4
    //N=2, number=11, return=3

    public int solution(int N, int number) {
        //N=5, number=12, return=4

        int cnt = 1;
        for (int i = 0; i < 100; i++) {
            int plus = N + N;
            int minus = N - N;
            int gob = N * N;
            int division = N / N;
            int overlap = N * 10;
        }

        int answer = 0;
        return answer;
    }
}
